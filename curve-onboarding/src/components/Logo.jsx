export default function Logo({ className = '' }) {
  return (
    <span
      className={`inline-flex items-baseline text-[20px] font-extrabold leading-none tracking-[-0.04em] text-bone ${className}`}
    >
      curve
    </span>
  )
}
